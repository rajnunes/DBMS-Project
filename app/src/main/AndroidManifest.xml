<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rajnunes.dbms">

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_hospital"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InsertActivity"
            android:label="Insert" />
        <activity android:name=".DisplayActivity" />
        <activity
            android:name=".DeleteActivity"
            android:label="Delete" />
        <activity
            android:name=".UpdateActivity"
            android:label="Update" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".InsertMenuActivity" />
        <activity android:name=".DoctorActivity" />
        <activity android:name=".NurseActivity" />
        <activity android:name=".DisplayMenuActivity" />
        <activity android:name=".DoctorDisplayActivity" />
        <activity android:name=".NurseActivityActivity" />
        <activity android:name=".DiagnosisActivity" />
        <activity android:name=".SearchIdActivity" />
        <activity android:name=".DiagnosisDisplayActivity"></activity>
    </application>

</manifest>